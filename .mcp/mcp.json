{
  "mcpServers": {
    "stripe": {
      "command": "bunx",
      "args": ["--bun", "@stripe/mcp", "--tools=all", "--api-key=${STRIPE_SECRET_KEY}"],
      "env": {
        "STRIPE_SECRET_KEY": "${STRIPE_SECRET_KEY}"
      }
    },
    "turso": {
      "command": "bunx",
      "args": ["--bun", "@turso/mcp"],
      "env": {
        "TURSO_DATABASE_URL": "${DATABASE_URL}",
        "TURSO_AUTH_TOKEN": "${DATABASE_AUTH_TOKEN}"
      }
    },
    "shadcn": {
      "command": "bunx",
      "args": ["--bun", "shadcn@latest", "mcp"]
    },
    "svelte": {
      "command": "bunx",
      "args": ["--bun", "sv", "mcp"]
    }
  }
}
